import matplotlib.pyplot as plt\nimport matplotlib.patches as mpatches\nimport matplotlib.lines as mlines\n\ndef generate_flowchart():\n    fig, ax = plt.subplots(figsize=(20, 10))  # Set figure size\n    ax.set_xlim(0, 10)  # Set x limits\n    ax.set_ylim(0, 5)  # Set y limits\n    ax.axis('off')  # Hide axes\n\n    # Define patches (rounded rectangles) for each module\n    multimodal_metadata_input = mpatches.FancyBboxPatch((0.5, 4), 2, 0.6, boxstyle="round,pad=0.1", color='lightgray')\n    encoding_enhancement_layer = mpatches.FancyBboxPatch((3.5, 3), 3, 1.3, boxstyle="round,pad=0.1", color='lightgray')\n    shared_vector_space = mpatches.FancyBboxPatch((6.5, 4), 2, 0.6, boxstyle="round,pad=0.1", color='lightgray')\n    training_objective_layer = mpatches.FancyBboxPatch((0.5, 1), 2, 0.6, boxstyle="round,pad=0.1", color='lightgray')\n    unified_metadata_management = mpatches.FancyBboxPatch((3.5, 0), 3, 1.3, boxstyle="round,pad=0.1", color='lightgray')\n\n    # Add patches to the ax\n    ax.add_patch(multimodal_metadata_input)\n    ax.add_patch(encoding_enhancement_layer)\n    ax.add_patch(shared_vector_space)\n    ax.add_patch(training_objective_layer)\n    ax.add_patch(unified_metadata_management)\n\n    # Define text labels for modules\n    ax.text(1.5, 4.3, '1) 多模态元数据输入', fontsize=16, ha='center')  # Main module\n    ax.text(1.5, 4.0, '4个子模块', fontsize=14, ha='center')\n    ax.text(5.0, 3.8, '2) 编码增强层', fontsize=16, ha='center')\n    ax.text(5.0, 3.5, '文本和视觉编码器 3个子模块和LoRA适配器', fontsize=14, ha='center')\n    ax.text(8.0, 4.3, '3) 共享向量空间构建', fontsize=16, ha='center')\n    ax.text(8.0, 4.0, '3个子组件', fontsize=14, ha='center')\n    ax.text(1.5, 1.3, '4) 训练目标层', fontsize=16, ha='center')\n    ax.text(1.5, 1.0, '2个损失函数和组合损失公式', fontsize=14, ha='center')\n    ax.text(5.0, -0.5, '5) 统一元数据管理', fontsize=16, ha='center')\n    ax.text(5.0, -0.8, '4个子模块和输出框', fontsize=14, ha='center')\n\n    # Draw arrows for data flow and supervision\n    ax.add_line(mlines.Line2D([2.5, 3.5], [4.3, 4.3], color='black', linewidth=2, linestyle='solid'))  # Data flow\n    ax.add_line(mlines.Line2D([5.5, 6.5], [3.8, 3.8], color='black', linewidth=2, linestyle='solid'))\n    ax.add_line(mlines.Line2D([0.5, 1.5], [1.6, 1.6], color='black', linewidth=2, linestyle='solid'))\n    ax.add_line(mlines.Line2D([2.5, 3.5], [1.3, 1.3], color='black', linewidth=2, linestyle='solid'))\n    ax.add_line(mlines.Line2D([5.5, 5.5], [3.3, 1.3], color='black', linewidth=2, linestyle='dashed'))  # Supervision arrow\n\n    # Set the title\n    plt.title('基于LoRA增强的元数据语义对齐与治理总体流程图', fontsize=20)\n    plt.tight_layout()  # Adjust layout\n    plt.show()\n\nif __name__ == '__main__':\n    generate_flowchart()\n